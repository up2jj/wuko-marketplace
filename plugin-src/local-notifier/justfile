build:
	go build -o wuko-plugin-local-notifier .

test:
	go test ./...

vet:
	go vet ./...

fmt:
	gofmt -w *.go tools/release/*.go

check: fmt vet test

release version:
	go run ./tools/release -version "{{version}}"
